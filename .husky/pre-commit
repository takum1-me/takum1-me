# ステージされたファイルだけを対象に整形と lint をかける。
# lint-staged が書き換えたファイルは自動で stage し直されるので、
# 「整形結果がコミットに入らない」という以前の問題は起きない。
#
# 型チェック（astro check）とビルドはリポジトリ全体を見る必要があって遅いので、
# ここでは走らせず CI に任せている。手元でまとめて確認したいときは npm run c-all。
npx lint-staged
