---
import Layout from "../layouts/Layout.astro";
import Container from "../components/shared/layout/Container.astro";
import PageHeader from "../components/shared/page/PageHeader.astro";
import { BeansShowcase } from "../components/features/beans";
import { getAllBeans, sortBeansForDisplay } from "../lib/microcms/beans-list";

const sorted = sortBeansForDisplay(await getAllBeans());
---

<Layout
  title="Beans"
  description="takum1 が焙煎しているコーヒー豆のリスト。フレーバーを色で記録しています。"
>
  <Container size="page">
    <PageHeader
      eyebrow="Beans"
      title="いま、焙煎している豆。"
      lead="少量ずつ選んだ生豆を、Kaffelogic nano 7 で焙煎しています。ここには、いま味わえるラインナップを記録しています。"
    >
      <p slot="aside">
        カード下部の色帯は、感じたフレーバーを重ねた味わいの輪郭です。気になる豆を開くと、精製方法や焙煎度も確認できます。
      </p>
    </PageHeader>

    <BeansShowcase beans={sorted} client:only="react" />
  </Container>
</Layout>
