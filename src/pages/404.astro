---
import Layout from "../layouts/Layout.astro";
import Button from "../components/shared/button/Button";
import "../components/shared/button/button.css";
---

<Layout title="ページが見つかりません">
  <main>
    <div class="error-content">
      <h1 class="error-title">404</h1>
      <h2 class="error-subtitle">ページが見つかりません</h2>
      <p class="error-message">
        お探しのページは存在しないか、移動された可能性があります。
      </p>
      <div class="error-actions">
        <Button href="/" className="button-error-primary">トップページに戻る</Button>
      </div>
    </div>
  </main>
</Layout>

<style>
  .error-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 60vh;
    text-align: center;
    padding: 2rem 1rem;
  }

  .error-title {
    font-size: 8rem;
    font-weight: 900;
    color: var(--color-text-primary);
    margin: 0;
    line-height: 1;
    letter-spacing: -0.02em;
  }

  .error-subtitle {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--color-text-secondary);
    margin: 1rem 0 0;
    line-height: 1.3;
  }

  .error-message {
    color: var(--color-muted);
    font-size: 1rem;
    font-weight: 600;
    line-height: 1.6;
    margin: 1.5rem 0 3rem;
    max-width: 25rem;
  }

  .error-actions {
    display: flex;
    justify-content: center;
  }

  .button-error-primary {
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease;
    animation: pulse-button 2s ease-in-out infinite;
    background-color: transparent;
    border: 2px solid var(--color-muted);
    color: var(--color-muted);
  }

  .button-error-primary::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 0;
    height: 100%;
    background-color: var(--color-muted);
    transition: width 0.5s ease-in-out;
    z-index: -1;
    transform-origin: left center;
  }

  .button-error-primary:hover::before {
    width: 100%;
  }

  .button-error-primary:hover {
    color: #fff;
    transform: translateY(-0.125rem);
    box-shadow: 0 0.5rem 1.5625rem rgb(107 114 128 / 30%);
  }

  @keyframes pulse-button {
    0%, 100% {
      transform: scale(1);
    }
    
    50% {
      transform: scale(1.05);
    }
  }

  /* レスポンシブデザイン */
  @media (width <= 48rem) {
    .error-content {
      min-height: 50vh;
      padding: 1rem 0.5rem;
    }

    .error-title {
      font-size: 6rem;
    }

    .error-subtitle {
      font-size: 1.3rem;
    }

    .error-message {
      font-size: 0.9rem;
      margin: 1rem 0 2rem;
    }
  }

  @media (width <= 30rem) {
    .error-title {
      font-size: 4rem;
    }

    .error-subtitle {
      font-size: 1.1rem;
    }
  }
</style>